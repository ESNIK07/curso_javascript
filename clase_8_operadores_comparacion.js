// == comparar si algo es igual a otra cosa
// === compara como == y ademas verifica que la igualdad sea en valor y en tipo de dato
// != diferente de
// !== igual que != y ademas verifica igualdad de valor y de tipo de dato
// > mayor que
// < menor que
// >= mayor igual que
// <= menor igual que
const a = 10
const b = 20
const c = '10'

a == b
